/*
function stopIt (e) {
    e.preventDefault();
    e.stopPropagation();
}

var a = document.querySelectorAll('.nav-link');
for (var i = 0; i < a.length; i++) {
    a[i].classList.add('external');
    a[i].removeEventListener("click", stopIt);
}
 */